<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Administración de Contactos | PixelCrafters</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <header>
        <h1>PixelCrafters</h1>
        <p>CREANDO MUNDOS VISUALES, UN PIXEL A LA VEZ</p>
        <nav class="main-nav">
            <button onclick="window.location.href='/'" class="nav-btn">Inicio</button>
            <button onclick="window.location.href='/contact'" class="nav-btn">Contacto</button>
            <button onclick="window.location.href='/payment'" class="nav-btn">Pago</button>
        </nav>
    </header>

    <div class="admin-section">
        <h2>Administración de Contactos</h2>
        <p>Revisa los mensajes recibidos de tus clientes</p>

        <div class="table-container">
            <table class="contacts-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nombres</th>
                        <th>Email</th>
                        <th>Asunto</th>
                        <th>Comentario</th>
                        <th>Fecha y Hora</th>
                        <th>País/IP</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (contacts && contacts.length > 0) { %>
                        <% contacts.forEach(contact => { %>
                            <tr>
                                <td><%= contact.id %></td>
                                <td><%= contact.names %></td>
                                <td>
                                    <a href="mailto:<%= contact.email %>" class="email-link">
                                        <i class="fas fa-envelope"></i>
                                        <%= contact.email %>
                                    </a>
                                </td>
                                <td><%= contact.subject %></td>
                                <td class="comment-cell"><%= contact.comment %></td>
                                <td class="date-cell">
                                    <i class="far fa-clock"></i>
                                    <%= contact.created_at %>
                                </td>
                                <td class="country-ip-cell">
                                    <i class="fas fa-globe-americas"></i>
                                    <span class="country-ip-text"><%= contact.country_ip %></span>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="7" class="no-contacts">
                                <i class="fas fa-info-circle"></i>
                                No hay mensajes de contacto registrados
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>